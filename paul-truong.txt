codepen link: https://codepen.io/paul-truong/pen/zYoMPWz?editors=1100

    1. What is Semantic HTML? HTML with tags that better describe the meaning of the code.
    2. What is HTML used for? HTML is a markup language to display data
    3. What is an attribute and where do we put it? An attribute defines properties of an element
    4. What is the h1 tag used for? How many times should I use it on a page? h1-6 is used as heading tags, with h1 being the largest and only used once. 
    5. Name two tags that have required attributes <a> <img>
    6. What do we put in the head of our HTML document? <!DOCTYPE html>
    7. What is an id? an id is a unique element selector
    8. What elements can I add an id to? anything
    9. How many times can I use the same id on a page? once
    10. What is a class? a class is like an id but doesn't have to be unique
    11. What elements can I add a class to? anything
    12. How many times can I use the same class on a page? As many times as you want
    13. How do I get my link to open in a new tab? target="_blank"
    14. What is the alt attribute in the image tag used for? shows text if the image cannot load
    15. How do I reference an id? #(id){}
    16. What is the difference between a section and a div? Section is like a big box and div is smaller boxes inside of the bigger box.
    17. What is CSS used for? to style a webpage. 
    18. How to we select an element? Example - every h2 on the page h2{}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one id is  for unique identifier class can be used to group multiple elements. Can us Id for h1, can use class for all subsequent h2's.
    20. How do we select classes in CSS? .(classname){}
    21. How do we select a p element with a single class of “human””? p.human{}
    22. What is a parent child selector? When would this be useful? Selects all child elements of the parent.  div > p{} would style all <p> tags under a div element. Could allow you to style some <p> tags but not all of them.
    23. How do you select all links within a div with the class of sidebar? div.sidebar{}
    24. What is a pseudo selector? A way to change a specific element such as :hover
    25. What do we use the change the spacing between lines? line-height:
    26. What do we use to change the spacing between letters? letter-spacing:
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform: uppercase; text-transform: capitalize; text-transform: lowercase; 
    28. How do I add a 1px border around my div that is dotted and black? div{ border: 10px dotted black;}
    29. How do I select everything on the page? *{}
    30. How do I write a comment in CSS? /**/
    31. How do I find out what file I am in, when I am using the command line? pwd
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? del /f filename, because it can delete multiple files at once
    34. Why should I use version control? So you can always go back to a previous version if the current does not work
    35. How often should I commit to github? Any time you make a change to a file
    36. What is the command we would use to push our repo up to github? git push -u origin (name)
    37. Walk me through Lambda's git flow. https://www.notion.so/GitFlow-step-by-step-a764163309754e23b1b9c4077b1653cc :)

Stretch Questions

    1. What is the difference between an inline element and a block element? A block element causes a line break
    2. What happens when an element is positioned absolutely? When an element is positioned absolutely it then becomes its own separate layer
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? padding
    4. Name 3 elements that are display block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default. <div>, <header>, <footer>. <a>, <br>. <img>.
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? The box model is a model that allows you to change the areas around the element and arrange the element in the the desired formation within the box. box-sizing: border-box;



   --> 